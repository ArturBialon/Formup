import { Injectable, computed, inject } from '@angular/core';
import { Router } from '@angular/router';
import { BehaviorSubject, Observable, of, tap } from 'rxjs';
import { catchError } from 'rxjs/operators';
import {
  AuthService,
  LoginCommand,
  UserResponse,
} from './autogenerated/autogenerated.service';
import { toSignal } from '@angular/core/rxjs-interop';

@Injectable({
  providedIn: 'root',
})
export class AccountService {
  private router = inject(Router);
  private authService = inject(AuthService);

  private userSubject = new BehaviorSubject<UserResponse | null | undefined>(undefined);
  user$: Observable<UserResponse | null | undefined> = this.userSubject.asObservable();

  private userSignal = toSignal(this.user$, { initialValue: undefined });
  isAdmin = computed(() => this.userSignal()?.role === 'Admin');
  isForwarder = computed(() => this.userSignal()?.role === 'Forwarder');
  isAccountant = computed(() => this.userSignal()?.role === 'Accountant');
  isVerifier = computed(() => this.userSignal()?.role === 'Verifier');
  isManagement = computed(() => this.userSignal()?.role === 'Management');
  userName = computed(() => this.userSignal()?.userName);

  constructor() {
    this.loadCurrentUser().subscribe();
  }

  userLogin(command: LoginCommand): Observable<UserResponse> {
    return this.authService.login(command).pipe(
      tap((user: UserResponse) => {
        this.setUser(user);
      })
    );
  }

  loadCurrentUser(): Observable<UserResponse | null> {
    const token = localStorage.getItem('token');
    if (!token) {
      this.userSubject.next(null);
      return of(null);
    }

    return this.authService.getCurrentUser().pipe(
      tap((user: UserResponse) => this.userSubject.next(user)),
      catchError(() => {
        this.logout();
        return of(null);
      })
    );
  }

  logout(): void {
    this.userSubject.next(null);
    localStorage.removeItem('token');
    this.router.navigateByUrl('/home');
  }

  private setUser(user: UserResponse): void {
    this.userSubject.next(user);
    if (user?.token?.accessToken) {
      localStorage.setItem('token', user.token.accessToken);
    }
  }
}
